# Public thumbnails

480px copies of gallery photos for the decorative `SurfPhoto` thumbnails on the public Cup and home pages (every `photo:` slug in `src/data/site.ts`). The gallery itself lives in `src/assets/gallery/` behind the member login; regenerate these from there if a slug changes.
