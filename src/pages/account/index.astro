---
export const prerender = false;
const session = Astro.locals.session!;
return Astro.redirect(session.member ? "/members" : session.archivedAt ? "/members/me" : session.family ? "/members/profile" : session.coach ? "/members/training" : "/account/profile");
---
