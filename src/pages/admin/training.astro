---
export const prerender = false;
// Training, coaches and point values moved into the club, where members see
// the roll and the leaderboard and organisers act on the same page.
return Astro.redirect(`/members/training${Astro.url.search}`);
---
