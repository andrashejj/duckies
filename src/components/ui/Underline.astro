---
import { cn } from "../../lib/cn";
import { underlineClass } from "../../lib/ui";
// Skewed highlighter stroke behind a word.
interface Props {
  class?: string;
}
const { class: className } = Astro.props as Props;
const classes = cn(underlineClass, className);
---
<span class={classes}><slot /></span>
